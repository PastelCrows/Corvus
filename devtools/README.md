# devtools/ — Phase 9

Developer tooling, installable through the GUI software center rather than
requiring manual setup.

Covers:
- Containerization support (Docker/Podman)
- Standard dev toolchain availability (compilers, language runtimes, version
  control)

See `docs/SCOPE.md` §11.

Status: not started.
